// Export user storage implementation using JSON files
export * as userRepo from './user.repo';

// Re-export types for convenience
export type { User, CreateUserData, UpdateUserData } from './user.repo';

